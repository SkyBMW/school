void setup(){
  //create window with hight 600 and width 500
  size(600, 500);
}
void draw(){
  //fill a colour
  fill(255, 0, 0);
  //fill a clour in an outline of the shape 
  stroke(0, 0, 255);
  //create an ellipse
  ellipse(300, 300, 100, 100);
  
}
